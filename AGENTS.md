# Agent Guidance

If this checkout is inside the EnclaveCode workspace, also read `../AGENTS.md`.

Before changing the website, read:
- `agent-guides/rules/tailwind-css.md`

When writing or editing blog posts, also read:
- `agent-guides/rules/blog-posts.md`

After changing Tailwind classes in HTML or Markdown, rebuild `assets/css/output.css` with `npm run build:css`.
